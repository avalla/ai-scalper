node_modules
.DS_Store
dist
coverage
.env
.env.local
.env.*.local
*.swp
.*.swp
apps/trader/data/
apps/trader/tmp/
# Operator-tuned local config — example template is tracked instead.
apps/trader/config.paper-pipeline.json
# Local tool state (per-host, not load-bearing for the trader).
**/.claude-flow/
.ai-office/
.swarm/
# IDE state.
.idea/
# Local sqlite/vector stores generated by tooling.
ruvector.db
# Supabase CLI scratch space (not used by the trader runtime).
supabase/
